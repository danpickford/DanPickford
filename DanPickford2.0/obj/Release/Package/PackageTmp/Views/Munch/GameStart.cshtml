@{
    Layout = "muchLayout.cshtml";
    ViewBag.Title = "Munch Game";
}
<script src='http://cdn.html5quintus.com/v0.1.1/quintus-all.min.js'></script>
<script>
     
      // Now set up your game (most games will load a separate .js file)
    var Q = Quintus()                          // Create a new engine instance
        .include("Sprites, Scenes, Input, 2D, Touch, UI") // Load any needed modules
        .setup("#munchBox")                           // Add a canvas element onto the page
        .controls()                        // Add in default controls (keyboard, buttons)
        .touch();                          // Add in touch support (for the UI)
              
      /*
        ... Actual game code goes here ... 
      */
   </script>
<canvas id="munchBox" class="munchBox"></canvas>